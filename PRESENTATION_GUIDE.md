# SkillPilot 시연 발표 가이드

> 이번 UI 리팩토링 + AI 학습 메이트 추가 작업을 발표용으로 정리.
> 두 부분으로 구성: ① **PPT 생성 프롬프트** ② **시연 영상에 담아야 할 장면 리스트**

---

## ① PPT 생성용 프롬프트

> ChatGPT / Gemini / Claude / Gamma 등 PPT 생성 AI에 그대로 붙여 쓰면 됨.
> 슬라이드 수, 톤, 분량 옵션을 상황에 맞게 조정.

### 🎯 풀버전 프롬프트 (복붙용)

```
역할: 너는 대학교 4학년 졸업/과제 발표용 PPT 디자이너야. "시연 중심"의 15분 발표용 슬라이드를 만들어줘.

프로젝트 이름: SkillPilot
한 줄 소개: AI 기반 IT 자격증 학습 코치 — 자격증 추천부터 주차별 로드맵, CBT 모의고사, 오답 해설, 모의면접, 채용 매칭, 포트폴리오까지 통합한 풀스택 웹 서비스
기술 스택: React 18 + Vite (Client) / Node.js + Express + Sequelize (MySQL) + Mongoose (MongoDB) / OpenAI API / data.go.kr 공공 데이터

이번 발표의 핵심 메시지:
1. "디자인 일관성 0 → 일관성 100" — CSS 토큰화 + 공통 컴포넌트 6종 도입
2. "기능은 있는데 안내가 없던 화면 → 학습자 옆에 항상 코치가 있는 경험" — 🦉 학습 메이트 (자동 응원 + 실제 OpenAI 챗봇)
3. "정보가 묻혀있던 대시보드 → 한눈에 보이는 진행 상황" — 통계 카드 4종 + SVG 도넛 + 학습 잔디밭 + 추이 차트
4. "접근성·반응형 사각지대 해결" — aria-*, 키보드 내비게이션, 모바일 480/768 브레이크포인트

발표 톤: 자랑하지 않고 "문제 → 해결 → 시연" 흐름. 글 적고, 화면 캡처/시연 영상이 메인.

슬라이드 구성 (총 15장):

1. **표지** — 제목 "SkillPilot · 학습자 옆에 코치가 있는 자격증 플랫폼" / 발표자 이름·소속·학번 / 한 줄 캐치프레이즈
2. **문제 정의** — 자격증 학습자가 겪는 3가지 문제: ① 어떤 자격증을 딸지 모름 ② 합격까지의 길이 보이지 않음 ③ 옆에서 응원/푸시해주는 사람이 없음 (이모지·아이콘 시각화)
3. **솔루션 한눈에** — SkillPilot 화면 한 장 + 6가지 핵심 기능 (자격증 추천 / 주간 로드맵 / CBT 모의고사 / AI 모의면접 / 채용 매칭 / 🦉 학습 메이트)
4. **기술 스택 & 아키텍처** — 간단한 다이어그램 (Client React → Express API → MySQL/MongoDB ↘ OpenAI). Recharts 추가 표기
5. **이번 업데이트의 목표** — Before/After 한 줄 요약: "각 페이지 따로 디자인 → 디자인 시스템 통일 + AI 코치 상시 + 대시보드 시각화"
6. **개선 1: 디자인 토큰** — 색상·radius·spacing·shadow를 CSS 변수로 통합. 코드 스니펫 + navy 팔레트 시각화
7. **개선 2: 공통 UI 컴포넌트 6종** — Button / Card / Input / Modal / Loading / EmptyState 카탈로그 (이미지 그리드)
8. **개선 3: 13개 페이지 일괄 마이그레이션** — Before/After 캡처 (Dashboard 모달, 폼 페이지, CBT, 모의면접 중 2~3개)
9. **개선 4: 접근성·반응형** — aria 라벨 / 키보드 포커스 링 / 480px·768px 모바일 캡처 3장
10. **개선 5: 🦉 학습 메이트 (시연 핵심)** — 알약 도크 캐릭터 + 말풍선 + 챗봇 패널 캡처 / mood 4종(cheer·scold·tip·idle) / 자동 트리거 10+가지를 표로
11. **개선 6: 챗봇 = 실제 AI 연결** — 백엔드 `/api/assistant/chat` 흐름 다이어그램 / typing indicator / 실패 시 OFFLINE mock fallback (시연 안전망)
12. **개선 7: 대시보드 가시화 (시연 하이라이트)** — Before/After 한 장. 통계 카드 4 + 응원 배너 + 도넛 + 학습 잔디밭 + 추이 차트 모두 한 화면
13. **개선 8: 모의면접 setup 리디자인** — Before(휑한 input) / After(🎤 + 3단계 + 빠른 직무 chips 8종) 한 장
14. **시연** — "지금 직접 보여드리겠습니다" 슬라이드 한 장 (시연 영상 또는 라이브 데모로 전환)
15. **잡은 버그 + 앞으로의 개선** — CBT conditional hooks 버그 / max_tokens 호환 자동 fallback / 향후: AI 응답 스트리밍 · 자동 멘트도 AI 생성

각 슬라이드 작성 규칙:
- 제목 한 줄 + 본문 3~5줄 이하 (글로 채우지 말 것)
- 화면 캡처가 들어갈 자리에는 [캡처: 어떤 화면] 으로 표시해줘
- 색상 팔레트: navy #2d3e5d, accent blue #3b82f6, surface white, danger #ef4444 — 이걸 슬라이드 마스터에도 적용
- 폰트: 한글 Pretendard, 영문 Inter
- 본문 텍스트 글머리표는 "·" 또는 굵은 키워드로 시작
- 발표자 노트(speaker notes)도 슬라이드별로 2~3문장 작성

출력 형식: 슬라이드별 마크다운 (## 슬라이드 N. 제목 / 본문 / [캡처/이미지 메모] / 발표자 노트:)
```

### 🪶 짧은 버전 (시간 부족할 때)

```
SkillPilot 프로젝트 시연 발표용 PPT 10장을 만들어줘.

핵심 메시지 3가지:
1. 디자인 시스템 도입 (CSS 토큰 + 공통 컴포넌트 6종)
2. AI 학습 메이트(목업) — 학습자를 따라다니며 응원·채찍·챗봇
3. 접근성·반응형·버그 수정

각 슬라이드는 제목 + 본문 3줄 + [캡처 자리] 형식.
색상은 navy/blue/white 톤. 글보다 화면 캡처 위주.
시연 슬라이드는 7번에 배치해줘 — "지금 직접 보여드리겠습니다" 한 장으로.
```

---

## ② 시연 영상에 담아야 할 장면 (촬영 콘티)

> 추천 순서: 사용자가 처음 가입해서 → 학습 시작 → AI 코치와 상호작용 → 결과 보기.
> 한 시퀀스가 끝날 때마다 어시스턴트의 변화도 같이 보이도록.

### 시퀀스 0. 인트로 (5초)
- 메인 페이지 접속 → 로그인 화면 첫 인상
- **포인트**: 로고 / 우측 폼 / 좌측 카피 — 새 토큰으로 정리된 색상감

### 시퀀스 1. 로그인 → 온보딩 흐름 (40초) ⭐ 디자인 통일성
| 화면 | 보여줄 것 |
|---|---|
| Login | 이메일 / 비밀번호 입력 → 로딩 스피너 (`<Button loading>`) |
| ProfileSetup | 직업 select → 학년/전공 동적 노출 → autocomplete 동작 |
| GoalSetting | 직무 카드 멀티 선택 → `aria-pressed` 토글 시각 효과 / 슬라이더 `aria-valuetext` |
| CertRecommendation | "추천 자격증을 불러오는 중" `<Loading>` → 카드 그리드 등장 → 카드 선택 → "선택 완료" |
| WeeklyRoadmap | "AI가 로드맵을 생성하고 있습니다" → 주차 카드들 등장 |

> **핵심 어필**: "로딩·빈 상태·에러 처리가 모든 페이지에서 같은 모습"이라는 것. 페이지가 바뀌어도 `<Loading>`/`<EmptyState>` 모양이 동일함을 보여줌.

### 시퀀스 2. 대시보드 진입 (60초) ⭐ 가시화 + 모달 + 어시스턴트 첫 등장
1. `/dashboard` 진입 → **🦉 학습 메이트가 우하단에서 환영 말풍선** "안녕하세요! 옆에서 도와드릴게요…"
2. **첫 화면 위에서 아래로 천천히 스크롤** — 보여줘야 하는 가시화 위젯들:
   - 통계 카드 4종 (연속 학습 / 이번 주 / 진행률 / 자격증) — 모두 동일 navy 톤의 미니멀 디자인
   - 응원 배너 7초 회전 (그대로 5~10초 멈춰서 한 번 전환되는 거 보여주기)
   - 로드맵 진행률 도넛 (그라디언트 stroke + 가운데 % 숫자)
   - 학습 기록 카드: 14일 추이 라인 차트 + 12주 학습 잔디밭 (셀 hover tooltip)
3. 자격증 카드 "상세보기" 클릭 → **새 `<Modal>` 오픈** (Portal + 부드러운 pop 애니메이션)
4. ESC 키 또는 오버레이 클릭 → 모달 닫힘
5. 사이드바 "메뉴" 햄버거 → 슬라이드 인 → 키보드 Tab으로 메뉴 이동 (포커스 링 강조)

> **핵심 어필**: 한 화면에 가시화 위젯 4종 + 모달 + 사이드바 + 어시스턴트가 모두 모인 "킬러 화면"

### 시퀀스 3. CBT 모의고사 (60초) ⭐ 어시스턴트 트리거 핵심
1. CBT 시작 클릭 → 메뉴 진입 시 어시스턴트 **`tip.cbt`** 멘트
2. 문제 풀이 시작 → 한 문제씩 선택 → 5번째 문제 도달 → **`cheer.streak` 자동 응원**
3. (시간 단축 위해 빨리 풀고) 답안 제출 → "정말 제출하시겠습니까?" → 확인
4. 결과 → `cheer.completed` 응원

> **편집 팁**: 90초 비활동 트리거를 보여주려면 1분 가만히 두기보다, 영상 따로 찍어 자막으로 "90초간 가만히 있으면 …"이라 표시하는 게 효율적

### 시퀀스 4. 오답 해설 (AnswerAnalyze) (30초)
1. 자동 진입 → "AI가 오답을 분석하고 있습니다" `<Loading>`
2. 점수 / 차트 / 오답 리스트 등장
3. **정답률에 따라 어시스턴트 멘트 분기**:
   - 80% 이상 → `cheer.completed`
   - 60% 이상 → `tip.cbt`
   - 그 외 → `scold.wrong` (채찍)
4. "로드맵 조정하기" 버튼 클릭 → 대시보드 복귀

> **핵심 어필**: "결과에 따라 코치가 다르게 반응한다"는 차별점

### 시퀀스 5. AI 모의면접 (45초) ⭐ 챗봇 동작 + 어시스턴트 점수 반응
1. 메뉴 → "AI 모의면접" 진입 → 어시스턴트 `tip.interview`
2. 직무 입력 → "면접 시작" → 첫 질문 등장
3. 답변 입력 → 전송 → AI 피드백 + 점수 + 다음 질문
4. **점수에 따라 어시스턴트 cheer/scold 자동 분기** 시연
5. 면접 종료 → `cheer.completed`

### 시퀀스 6. 학습 메이트 챗봇 (45초) ⭐⭐ 시연 최대 하이라이트
1. 우하단 🦉 알약 도크 클릭 → 챗 패널 오픈 (펄스 ring 자연스럽게 사라짐 강조)
2. **자유 질문 입력** (mock으론 안 되는 것): "이번 주말에 SQL이랑 자바 중 뭐 공부하는 게 좋을까요?"
3. **typing indicator (•••) 잠깐 → AI 답변** (1~3문장, 진짜 코칭 톤)
4. 한두 번 더 자유 대화 — 문맥이 이어지는 모습 (history 10턴)
5. (옵션) 백엔드 끄거나 네트워크 끊고 다시 입력 → **OFFLINE 배지 + mock 응답으로 자동 fallback** 시연
6. ESC → 패널 닫힘 → 안 읽은 메시지 뱃지 시연

> **핵심 어필**: 챗봇은 진짜 OpenAI, 자동 응원은 mock. 발표 중 네트워크 끊겨도 OFFLINE fallback 으로 시연 안 끊김. 시연 안전망 설계가 어필 포인트

### 시퀀스 7. 반응형 (모바일) (20초) ⭐ 빠르게 컷
- DevTools → Responsive 모드 → 480px / 768px / 1280px 빠르게 전환
- Dashboard / Login / CBT 메뉴 — 깨짐 없이 자연스럽게 재배치되는 모습
- 어시스턴트 알약이 모바일에서 작아지는 것까지

### 시퀀스 8. 접근성 데모 (옵션, 15초)
- 마우스 빼고 Tab 키만으로 사이드바 메뉴 이동
- 포커스 링이 명확히 표시되는 모습
- 모달 안에서 ESC로 닫기

### 시퀀스 9. 아웃트로 (5초)
- 대시보드 전체 화면 + "감사합니다" 자막

---

## 📋 영상 편집/PPT 합본 추천 흐름 (총 5분)

| 시간 | 내용 |
|---|---|
| 0:00 ~ 0:25 | 인트로 + 시퀀스 1 압축 (온보딩 흐름) |
| 0:25 ~ 1:25 | **시퀀스 2 (대시보드 가시화 + 어시스턴트 첫 등장)** ⭐ |
| 1:25 ~ 2:15 | 시퀀스 3 (CBT — 어시스턴트 자동 응원) |
| 2:15 ~ 2:40 | 시퀀스 4 (오답 해설 — 결과별 분기) |
| 2:40 ~ 3:25 | 시퀀스 5 (모의면접 — 새 setup UI 강조) |
| 3:25 ~ 4:10 | **시퀀스 6 (실제 AI 챗봇 시연)** ⭐⭐ |
| 4:10 ~ 4:30 | 시퀀스 7 (반응형) |
| 4:30 ~ 4:50 | 시퀀스 8 (접근성) |
| 4:50 ~ 5:00 | 아웃트로 + 마지막 슬라이드 |

---

## 🎬 촬영 팁

- **마우스 커서 키우기**: macOS 시스템 환경설정 > 손쉬운 사용 > 포인터 크기 키움
- **클릭 시각화**: QuickTime + 화면 녹화 시 키스트로크/마우스 클릭 표시 옵션
- **창 크기 통일**: 화면 녹화는 1280×800 또는 1440×900으로 고정 — PPT 임베드 시 잘림 방지
- **30fps 이상**: 어시스턴트 펄스/말풍선 등장 모션이 끊겨 보이지 않게
- **사운드**: 무음 + 자막 자막 위주 (슬라이드 한 줄 + 화살표로 강조하는 편이 발표장에서 잘 들림)
- **라이브 데모 vs 녹화 영상**: 라이브는 위험. 녹화 영상 + 일부 단계만 라이브로 (특히 챗봇 입력 부분)

---

## 🗂️ 발표용 핵심 한 문장 정리

> 발표 도중 "이번 작업의 의미"를 묻는다면 이 한 줄로:

> **"기능은 이미 있던 SkillPilot에, 학습자가 끝까지 따라올 수 있도록 일관된 디자인 언어와 옆에서 같이 가는 코치, 그리고 한눈에 보이는 진행 상황을 더했습니다."**

---

## 📦 부록 — 시연 전 체크리스트

발표 직전 5분 전에 한 번 체크.

```
□ 백엔드 서버 켜짐 (npm run dev)              — 챗봇 AI 응답 위해 필수
□ 프론트 서버 켜짐 (npm run dev)              — 5173 포트
□ MySQL / MongoDB 켜짐                         — brew services list
□ .env 의 OPENAI_MODEL = gpt-4o-mini (또는 신모델)
□ OPENAI_API_KEY 잔액 확인 (챗 한 턴당 ~$0.0005)
□ 학습 기록 더미 데이터 입력해뒀는지            — 잔디밭/추이 차트 비어있으면 임팩트 0
□ 자격증 1개 선택 + 로드맵 생성 완료 상태       — 진행률 도넛 채우려면 필요
□ DevTools 닫고 브라우저 zoom 100%             — 캡처 통일
□ 네트워크 모드 정상 (OFFLINE fallback 보여주려면 마지막에 끄기)
□ 마우스 커서 크게 (시스템 환경설정)
```
